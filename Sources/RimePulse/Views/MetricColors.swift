import SwiftUI

enum MetricColors {
    static let totalChars = Color.primary
    static let charsCjk = Color.cyan
    static let wordsEn = Color.purple
    static let charsPerMinute = Color.orange
    static let activeMinutes = Color.blue
    static let commits = Color.green
}
